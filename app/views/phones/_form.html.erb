<%= form_with model: @phone, id: "form" do |form| %>

  <% if @phone.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phone.errors.count, "error") %> prohibited this phone from being saved:</h2>

      <ul>
        <% @phone.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

    <div>
      <div class="require">*<%= form.label :Phone_number %></div>
      <%= form.text_field :phone_number, :size => 40 %><br>
      <%= form.label :Comment %>
      <%= form.text_field :comment %>
      <br>
    </div>

  <div class="actions">
    <%= form.submit %>
  </div>
  
<% end %>
